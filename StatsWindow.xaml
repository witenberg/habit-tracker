<Window x:Class="HabitTracker.StatsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Statystyki nawyku" Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek -->
        <TextBlock Grid.Row="0" x:Name="HabitNameTextBlock" 
                   Text="Statystyki" FontSize="24" FontWeight="Bold" 
                   Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <!-- Opis nawyku -->
        <TextBlock Grid.Row="1" x:Name="HabitDescriptionTextBlock" 
                   Text="" FontSize="12" Foreground="Gray" 
                   TextWrapping="Wrap" Margin="0,0,0,20" 
                   HorizontalAlignment="Center"/>

        <!-- Statystyki -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="StatsPanel">
                <!-- Statystyki będą dodawane dynamicznie w code-behind -->
            </StackPanel>
        </ScrollViewer>

        <!-- Przycisk zamknięcia -->
        <Button Grid.Row="3" Content="Zamknij" Width="100" Height="35" 
                Click="CloseButton_Click" 
                HorizontalAlignment="Center" Margin="0,20,0,0"
                Background="#2196F3" Foreground="White"
                FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
    </Grid>
</Window>
